import {
    mdiCog,
    mdiFormatListBulleted,
    mdiLogout,
    mdiMenu,
    mdiRefresh,
} from "@mdi/js";

export class Icons {
    public static readonly cog = mdiCog;
    public static readonly formatListBulleted = mdiFormatListBulleted;
    public static readonly logout = mdiLogout;
    public static readonly menu = mdiMenu;
    public static readonly refresh = mdiRefresh;
}
